# .github/workflows/scrape-qa-security-database.yml
name: Scrape QA, Security & Database

on:
  push:
    branches: [ main ]
    paths:
      - 'main.py'
      - 'jobspy/**'
      - 'requirements.txt'
      - '.github/workflows/scrape-qa-security-database.yml'
  schedule:
    - cron: '0 9 */2 * *'  # Runs at 9 AM UTC every 2 days
  workflow_dispatch:

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 25

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run scraper for QA & Testing
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: |
          python main.py qa_testing

      - name: Run scraper for Security
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: |
          python main.py security

      - name: Run scraper for Database
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: |
          python main.py database

      - name: Upload logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: scraper-logs-qa-security-database
          path: |
            scraper.log
            *.csv
          retention-days: 7

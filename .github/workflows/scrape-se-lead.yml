# .github/workflows/scrape-se-lead.yml
name: Scrape SE Lead & Architect

on:
  push:
    branches: [ main ]
    paths:
      - 'main.py'
      - 'jobspy/**'
      - 'requirements.txt'
      - '.github/workflows/scrape-se-lead.yml'
  schedule:
    - cron: '0 4 */2 * *'  # Runs at 4 AM UTC every 2 days
  workflow_dispatch:

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run scraper for SE Lead & Architect
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: |
          python main.py se_lead_architect

      - name: Upload logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: scraper-logs-se-lead
          path: |
            scraper.log
            *.csv
          retention-days: 7

# .github/workflows/scrape-software-engineering.yml
name: Scrape Software Engineering Jobs

on:
  push:
    branches: [ main ]
    paths:
      - 'main.py'
      - 'jobspy/**'
      - 'requirements.txt'
      - '.github/workflows/scrape-software-engineering.yml'
  schedule:
    - cron: '0 0,12 * * *'  # Runs at midnight and noon UTC
  workflow_dispatch:

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run scraper for software engineering jobs
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: |
          python main.py software_engineering

      - name: Upload logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: scraper-logs-software-engineering
          path: |
            scraper.log
            *.csv
          retention-days: 7
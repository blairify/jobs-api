# .github/workflows/scrape-se-general.yml
name: Scrape SE General US

on:
  push:
    branches: [ main ]
    paths:
      - 'main.py'
      - 'jobspy/**'
      - 'requirements.txt'
      - '.github/workflows/scrape-se-general.yml'
  schedule:
    - cron: '0 0 */2 * *'  # Runs at 12 AM UTC every 2 days
  workflow_dispatch:

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run scraper for SE General US
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: |
          python main.py se_general_us

      - name: Upload logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: scraper-logs-se-general
          path: |
            scraper.log
            *.csv
          retention-days: 7

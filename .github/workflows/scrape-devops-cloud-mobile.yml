# .github/workflows/scrape-devops-cloud-mobile.yml
name: Scrape DevOps, Cloud & Mobile

on:
  push:
    branches: [ main ]
    paths:
      - 'main.py'
      - 'jobspy/**'
      - 'requirements.txt'
      - '.github/workflows/scrape-devops-cloud-mobile.yml'
  schedule:
    - cron: '30 8 */2 * *'  # Runs at 8:30 AM UTC every 2 days
  workflow_dispatch:

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 25

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run scraper for DevOps
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: |
          python main.py devops

      - name: Run scraper for Mobile
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: |
          python main.py mobile

      - name: Upload logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: scraper-logs-devops-cloud-mobile
          path: |
            scraper.log
            *.csv
          retention-days: 7
